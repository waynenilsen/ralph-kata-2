model Tenant {
  id        String   @id @default(cuid())
  name      String
  users     User[]
  todos     Todo[]
  invites   Invite[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
